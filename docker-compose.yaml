#https://learn.microsoft.com/pt-br/sql/linux/quickstart-install-connect-docker?view=sql-server-ver16&pivots=cs1-bash
version: "3"
services:
  php-ssl:
    build:
     context: ./
     dockerfile: Dockerfile
    networks:
      - frontend
      - backend
    restart: always
    ports:
     - 9080:80
     - 9443:443
    volumes:
     - ./www/:/var/www/html

    depends_on:
     - db
    links:
     - db

  db:
    #container_name: sql1
    #image: mcr.microsoft.com/mssql/server:2022-latest
    #networks:
    # - backend
    #restart: always
    #ports:
    # - "1433:1433"
    #environment:
    # - MSSQL_SA_PASSWORD=Rh@b74@@

    #volumes:
    # - ./db:/var/lib/sql1

  #sqlserver:
    container_name: sql1
    image: mcr.microsoft.com/mssql/server:2019-latest
    networks:
      - backend
    environment:
      SA_PASSWORD: "Rh@b74@@2023"
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - ./db:/var/lib/sql1

  mssqltools:
    image: mcr.microsoft.com/mssql-tools
    depends_on:
      - db
    volumes:
      - ./src/01-BaseGeografica.sql:/tmp/01-BaseGeografica.sql
      - ./src/02-BaseGeografica.sql:/tmp/02-BaseGeografica.sql
      - ./src/init-basegeografica.sh:/tmp/init-basegeografica.sh
    command: /bin/bash ./tmp/init-basegeografica.sh


networks:
   frontend:
   backend:

volumes:
   db:


